<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MoneyManager.Views.AccountTypeList"
             
             xmlns:model="clr-namespace:MoneyManager.Models"
             xmlns:viewModel="clr-namespace:MoneyManager.ViewModels">


    <Picker Title="Select Account Type"
            ItemsSource="{Binding _accountTypes}"
            ItemDisplayBinding="{Binding accountTypes}">
        
    </Picker>


    <!--<CollectionView ItemsSource="{Binding _accountTypes}">
        <CollectionView.ItemTemplate>
            <DataTemplate x:DataType="model:AccountType">
                <VerticalStackLayout>
                    <Label Text="{Binding accountTypes}"/>
                </VerticalStackLayout>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>-->

    <!--<ListView ItemsSource="{Binding _accountTypes}">
        <ListView.ItemTemplate>
            <DataTemplate x:DataType="model:AccountType">
                <Label Text="{Binding accountTypes}"/>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>-->
    
    
 
</ContentView>
